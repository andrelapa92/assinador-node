services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: node-api-backend
    volumes:
      - ./output:/app/resources/arquivos
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
